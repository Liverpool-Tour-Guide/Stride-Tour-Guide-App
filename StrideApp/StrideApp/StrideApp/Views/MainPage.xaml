<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="StrideApp.MainPage"
             Title="Where would you like a tour?">
    <ScrollView>
        <StackLayout BindableLayout.ItemsSource="{Binding Cities}" Spacing="0">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Grid HeightRequest="250" Margin="0,0">
                        <Grid.GestureRecognizers><TapGestureRecognizer Tapped="CitySelected_Tapped" ></TapGestureRecognizer></Grid.GestureRecognizers>
                        <Image Source="{Binding ImageSource}" Aspect="AspectFill"/>
                        <Label Text="{Binding UpperCaseName}" TextColor="White" VerticalOptions="Center" HorizontalOptions="Center" FontSize="60"/>
                    </Grid>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
    </ScrollView>
</ContentPage>

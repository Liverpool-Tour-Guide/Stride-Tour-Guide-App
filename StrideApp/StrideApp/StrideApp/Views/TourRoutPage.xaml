<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="StrideApp.Views.TourRoutPage"
             Title="Route">
    <ContentPage.Content>
        <ListView Margin="10,5"
                    ItemsSource="{Binding Waypoints}"
                    HasUnevenRows="true">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button BackgroundColor="{Binding MarkColor}"
                                    HorizontalOptions="Center"
                                    BorderWidth="1"
                                    BorderRadius="100"
                                    WidthRequest="50"
                                    HeightRequest="50"
                                    Grid.RowSpan="2"
                                    Grid.Row="0" />

                            <Label Text="{Binding Name}"
                                   TextColor="{Binding TextColor}"
                                   FontSize="Large"
                                   FontAttributes="Bold"
                                   Grid.Row="0"
                                   Grid.Column="1" />
                            <Label Text="{Binding Description}"
                                   VerticalOptions="End"
                                   Grid.Row="1"
                                   Grid.Column="1" />
                            <Button x:Name="btnPlay"
                                    ClassId="{Binding StorageIndex}"
                                    Text="Play"
                                    BorderColor="Green"
                                    Grid.Column="2"
                                    Pressed="OnPlayButtonClicked"
                                    Released="ToggleClickedHandler"
                                    />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>